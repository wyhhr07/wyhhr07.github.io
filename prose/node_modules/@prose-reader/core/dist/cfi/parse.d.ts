export type ProseParsedCfi = {
    isCfiRange: boolean;
    itemIndex: number;
};
export declare const isRootCfi: (cfi: string) => boolean;
export declare const parseCfi: (cfi: string) => ProseParsedCfi & {
    offset: number;
};
