import { Manifest } from '@prose-reader/shared';
import { Observable } from 'rxjs';
import { Context } from '../../../context/Context';
import { ReaderSettingsManager } from '../../../settings/ReaderSettingsManager';
export declare const loadAssets: ({ settings, item, context, }: {
    settings: ReaderSettingsManager;
    item: Manifest["items"][number];
    context: Context;
}) => (stream: Observable<HTMLIFrameElement>) => Observable<HTMLIFrameElement>;
export declare const unloadAssets: (frameElement?: HTMLIFrameElement) => void;
