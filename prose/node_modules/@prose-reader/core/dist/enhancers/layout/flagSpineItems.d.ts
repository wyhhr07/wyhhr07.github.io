import { Reader } from '../../reader';
import { Observable } from 'rxjs';
import { SpineItem, SpineItemState } from '../..';
export declare const flagSpineItems: (reader: Reader) => Observable<{
    item: SpineItem;
} & SpineItemState>;
