import { Archive } from '../..';
export declare const generateResourceFromArchive: (archive: Archive, resourcePath: string) => Promise<{
    body: string | Blob;
    params: {
        contentType?: string;
    };
}>;
