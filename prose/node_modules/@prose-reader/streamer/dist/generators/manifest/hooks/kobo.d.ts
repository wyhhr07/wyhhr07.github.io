import { Manifest } from '@prose-reader/shared';
import { Archive } from '../../../archives/types';
export declare const kobo: ({ archive }: {
    archive: Archive;
    baseUrl: string;
}) => (manifest: Manifest) => Promise<Manifest>;
